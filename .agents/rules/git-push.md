# Git Push Policy

Do NOT push changes to the remote Git repository (`git push`) unless the user explicitly asks or gives permission to do so.

- Local builds, tests, edits, and commits (if appropriate) are allowed locally, but NEVER run `git push` without explicit user instruction.
