# backend/envelope

Setback-derived buildable envelope.

> [!warning] Known gap
> Setback values are hardcoded for now. Real values come from local building bye-laws and vary
> by plot size and road width. Do not build logic that assumes any fixed setback is correct —
> see [[environment-notes]]. Keep values in one data module with the source cited per value.

Not started — needed by [[step-2-solver-core]] to bound the CP-SAT domains.
