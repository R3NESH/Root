# backend/vaastu

Vaastu direction rules as CP-SAT constraints, not a post-hoc score. Implements
[[vaastu-as-constraints]]. Demand-side rationale: [[vaastu-is-mandatory-demand]].

Not started — lands at [[step-5-vaastu]]. v1 rule set: kitchen SE, master bedroom SW,
entrance N/E, relative to the facing set in `frontend/input/`.
