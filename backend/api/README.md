# backend/api

`POST /solve` — the single endpoint. Request/response shape: [[output-schema]].
Stateless, no database, no secrets: [[environment-notes]].

Not started — lands at [[step-2-solver-core]] (stub), [[step-3-wire-together]] (wired to the
frontend, debounced ~400ms).
