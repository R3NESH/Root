---
tags: [daily]
date: 2026-08-23
---
# 2026-08-23

## Working on

Vault setup, then a corrected phasing scope, then [[step-1-threejs-shell]] — built and verified.

## Findings

- Project handoff captured verbatim at [[HANDOFF]] and split into linked notes. That file stays
  the source of truth; notes cite it by section.
- Obsidian was **not** installed on this machine. Installed via
  `winget install Obsidian.Obsidian --scope user`, exit 0.
- Vault root set to the **repo root**, not a `docs/` subfolder, so future module READMEs join the
  same graph as the design notes. Convention written up in [[codebase-map]].
- Repo directory named `plot-to-plan` as a provisional working name — [[HANDOFF]] left it unset.
  Not a real decision: [[project-name]].
- **Phasing corrected**: Phase 1 = generate a real 3D house model, not a 2D plan under an
  extruded box. Phase 2+ = multi-level with fire exits/egress/staircases hardcoded as solver
  constraints. See [[project-phases]], [[HANDOFF]] §13. Saved as project memory.
- Local Python is 3.14 — **risk closed**: `ortools==9.15.6755` has a native `cp314` wheel,
  installs cleanly into `backend/.venv`. See [[environment-notes]].
- Scaffolded `frontend/` (Next.js 16.3.2 + TypeScript + Three.js 0.185.1) and `backend/`
  (module skeleton READMEs only, no solver code yet — that's [[step-2-solver-core]]).
- Next.js 16's `AGENTS.md` warned the App Router differs from training data. Checked
  `node_modules/next/dist/docs/` before writing `page.tsx` — client/server component model is
  unchanged from what's documented; no other surprises hit.
- **[[step-1-threejs-shell]] built and verified**, not just typechecked: plot box, dashed
  setback envelope, translucent buildable-volume extrusion, two independent drag handles
  (snap-to-foot), preset cards, custom steppers, 8-direction compass dial, live buildable-area
  readout. Verified with a temporary Playwright + Chromium install against the running dev
  server — screenshots, DOM assertions, and a real drag simulation, not just `tsc`/`eslint`.
  Caught and fixed one real bug this way: the camera didn't reframe on a preset/stepper jump
  (30×40 → 50×80 left the box poking out of view). Fixed by recomputing camera distance from
  plot bounding radius while preserving the user's orbit angle. Playwright and all scratch
  scripts were removed after verification — not a project dependency.
- New react-hooks lint rule (`react-hooks/refs`, React 19 / eslint-config-next 16) rejects
  mutating `ref.current` during render — moved those assignments into a `useEffect`. Worth
  remembering for any future component using the "ref mirrors latest prop for a stable event
  handler" pattern.

## Measurements

| what | value | vs baseline |
|---|---|---|
| tests run | none (no test suite yet — the Playwright checks above were manual/scratch) | [[test-baseline]] does not exist yet |
| `tsc --noEmit` | 0 errors | — |
| `eslint` | 0 errors, 0 warnings | — |
| browser console errors across all interactions | 0 | — |
| drag-handle snap accuracy | exact — e.g. 40→47 ft drag, buildable 30→37 ft (matches 5 ft front/rear setback) | — |

## Blocked on

Nothing technically for step 1–2. Two zero-code questions still outrank steps 3–5:

- [[q-competitor-defects]] — an hour of work; two of its five outcomes invalidate steps 4–5
- [[q-does-anyone-pay]] — ten WhatsApp conversations, ₹500 each

## Tomorrow

[[step-2-solver-core]] — CP-SAT room placement in a fixed 30×40 envelope; this is where
[[test-baseline]] gets established for real. Consider answering [[q-competitor-defects]] first —
it may redirect step 5's Vaastu constraints and step 4's stability work before either is written.
If visual verification keeps being needed, run `/run-skill-generator` to capture today's ad-hoc
Playwright setup as a reusable project skill instead of reinstalling it each time.
