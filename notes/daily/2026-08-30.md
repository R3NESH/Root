---
tags: [daily]
date: 2026-08-30
---
# Daily Note — 2026-08-30

## Work Accomplished Today

### 1. Expanded 20-Blueprint CAD Architectural Catalog
- Researched real-world residential floor plans across the web and integrated **20 authentic, professionally engineered architectural floor plans** into [[modelBlueprints.ts]]:
  - **1BHK–4BHK layouts** across standard plot dimensions (20×30, 20×40, 25×40, 25×50, 30×40, 30×50, 35×50, 36×48, 40×60, 50×80).
  - **Directional Vastu Orientations**: Dedicated North, East, South, and West-facing floor plans.
  - **Heritage & Contemporary Styles**: Traditional Kerala Courtyard (Nalukettu) Villa, Chettinad Heritage Manor, Scandinavian Modernist Villa, Japanese Zen Minimalist Villa, and Parisian Haute Penthouse.
- Expanded [[ModelBlueprintsModal.tsx]] filter bar with dedicated Plot Size pills, Layout Type pills, Directional Facing pills (🧭 North, 🌅 East, ☀️ South, 🌇 West), and live instant search.

### 2. Curated Contiguous Coordinate Snapping (`customPositions`)
- Replaced naive grid cell centering in `solveClientSide` with exact, flush contiguous coordinate matrices for every blueprint.
- Every room shares its boundary walls with adjoining rooms with **0-inch gaps**, eliminating:
  - Detached rooms (`BATH`, `POOJA`) floating in outer garden setbacks.
  - Parallel double walls and narrow alleyways between living areas.
- Added `setRoomPositions()` to [[useSolve.ts]] to seed the solver's persistent instance map directly upon blueprint selection.

### 3. Window on Partition Wall & Interior Doorway Resolution
- Fixed issue in [[Scene.tsx]] where windows appeared on interior partition walls looking into adjacent rooms.
- Enforced strict exterior-only window constraint: `!isShared && !hasDoor && !hasFullOpening ? windowOn(i, edge) : undefined`.
- Enhanced `assignedDoorways` in [[Scene.tsx]] to automatically discover touching adjacent rooms and place centered interior doorways between them.

### 4. Awning (Chajja) Alignment
- Rewrote roof chajja generation in [[Scene.tsx]]: built directly inside `buildWall` on exterior walls only, centered precisely above window meshes (`wx` on N/S, `wz` on E/W) and doorframes (`doorPos`).

### 5. Solver & Vaastu Bug Fixes
- **Pooja Vaastu Coordinates**: Corrected Ishanya North-East quadrant coordinates in [[rules.py]] to `x: 0.5..1.0, z: 0.0..0.5`.
- **Relaxation Ladder Bounds**: Clamped custom room minimum dimensions against catalog minimums in [[model.py]].
- **Entrance Foyer Placement**: Added `PARENT_PREFERENCE["entrance"]` in [[connectivity.py]] prioritizing front doors on dedicated foyers.

---

## Verification & Metrics
- **Backend Test Suite**: **43 / 43 tests passing (100%)** in pytest.
- **Frontend Build**: `npm run build` compiled with **0 TypeScript errors** and static optimization passed.
- **Git Tree**: Clean, all changes committed (`2871114`) and pushed to `origin/main`.

---

## Session 2 — structurize, graph rebuild, agent rules

### 6. Structurize pass on the three largest files
Five modules lifted out of `Scene.tsx`, `Blueprint2DView.tsx` and `page.tsx` — all mechanical
moves, no logic touched. Full table and line counts in [[codebase-map]].

- `lib/sceneConstants.ts`, `lib/sceneBadges.ts`, `lib/sceneDoorways.ts` out of [[Scene.tsx]]
- `lib/blueprint2dPresets.ts` out of [[Blueprint2DView.tsx]] — six preset arrays that were
  literals inline in the JSX, rebuilt on every render
- `lib/projectStorage.ts` out of [[page.tsx]] — the `localStorage` key was a magic string in
  three places

Two duplications removed: `Scene.tsx`'s private `snapToFoot()` / `clampInches()` were
byte-equivalent to the ones in `lib/units.ts` it already imported from, and the CAD tool union
was written out verbatim in four files (now `CadTool`).

**Verification.** `tsc --noEmit` clean · `next build` exit 0 · **43/43** pytest ·
`eslint` problem counts *identical* to the pre-change baseline (20 and 17). The eslint parity
check is the one that matters — with no frontend tests, unchanged-warning-count is the only
evidence a "pure move" was pure.

### 7. Knowledge graph rebuilt — and the documented trap sprang
548 → **662** nodes, 1,319 → **1,547** edges. But a plain `graphify update .` silently dropped
`notes/build/` to **0 nodes**, exactly as [[knowledge-graph]] warned on 2026-08-25 — taking
[[test-baseline]] with it. Restored by patching `_SKIP_DIRS` in-process; the note now carries
the runnable fix and a two-line check to run after every rebuild.

`graphify label` failed (no LLM backend on `$PATH`), so community names are hub-derived this
run. Structure is right, labels are uglier.

### 8. Root `CLAUDE.md` added
Agent instructions for the repo: a terse voice rule, four behavioural rules (think before
coding, simplicity, surgical changes, goal-driven verification), and the project-specific parts
— integer inches, Vaastu-as-constraint, never drop connectivity, never `git push`, and the
vault convention. Merges with the existing [[ponytail]] ladder rather than restating it.

### 9. Finding: the client-side fallback lies
[[client-side-fallback]] — `solveClientSide()` reports `"Vastu Solved (Optimal)"` and four
Vaastu rules it never enforces, fires on any non-`ok` response, and is what a deployed Netlify
visitor actually gets because `NEXT_PUBLIC_SOLVER_URL` is unset. `frontend/README.md` claimed
this fallback did not exist; corrected. **Not fixed — recorded.**

---

Related: [[project-status]] · [[codebase-map]] · [[knowledge-graph]] · [[client-side-fallback]] · [[daily-log]]
